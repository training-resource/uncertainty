#!/bin/bash
echo "RStudio Server is running. Click the 'Ports' tab below and open port 8787 in a browser to access RStudio."
# The RStudio server might need an explicit start command depending on the base image, 
# but the rocker images usually handle this automatically. The ports tab should become active.

